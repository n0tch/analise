<p id="notice"><%= notice %></p>

<h1>Listing Cursos</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Flgstatus</th>
      <th>Inseridopor</th>
      <th>Datadeatualizacao</th>
      <th>Atualizadopor</th>
      <th>Datadeatualizacao</th>
      <th>Idioma</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cursos.each do |curso| %>
      <tr>
        <td><%= curso.nome %></td>
        <td><%= curso.flgStatus %></td>
        <td><%= curso.inseridoPor %></td>
        <td><%= curso.dataDeAtualizacao %></td>
        <td><%= curso.atualizadoPor %></td>
        <td><%= curso.dataDeAtualizacao %></td>
        <td><%= curso.idioma.nome %></td>
        <td><%= link_to 'Show', curso %></td>
        <td><%= link_to 'Edit', edit_curso_path(curso) %></td>
        <td><%= link_to 'Destroy', curso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Curso', new_curso_path %>
